apiVersion: sql.tanzu.vmware.com/v1
kind: Postgres
metadata:
  name: postgres-db
spec:
  memory: 800Mi
  cpu: "0.8"
  storageClassName: standard
  storageSize: 3G
  serviceType: LoadBalancer
  highAvailability:
    enabled: true